# This is where you construct your agents. 
# A file like research_agent.py would import the necessary tools (from /tools/) and the prompt (from /prompts/), 
# and then use LangChain functions (like create_openai_tools_agent) to build and return the final AgentExecutor.